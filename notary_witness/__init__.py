"""Independently verify an append-only transparency log.

This package deliberately contains only what is needed to *check* a log, never
to operate one. An auditor should be able to read all of it.
"""

__version__ = "0.1.0"
